# 5.29组会：

#### 内容概括：

大家在讨论：我们是让大模型直接预测信道地图，

还是让它学会电磁传播过程，或者把它当作工具调度器。

# 具体内容：

### 一.组会先讲了 RFT / CoT 是什么

**1.CoT = Chain of Thought，思维链。**

最简单理解：

> 不是让模型直接给答案，而是让它把中间推理步骤写出来。

**2.RFT = Reinforcement Fine-Tuning，强化微调。**

> **不只是让模型模仿标准答案，而是通过“奖励”让模型学会更好的推理行为。**

###### 普通监督微调 SFT 是：

###### 给模型一个输入；给它一个标准答案；让它学会模仿这个答案。

**3.RLHF = Reinforcement Learning from Human Feedback。**

意思是：

> 用人类反馈来训练模型。

**4.RM = Reward Model，奖励模型。**

它的作用是：

> 模拟人类打分。

一开始需要人类判断哪个回答好，哪个回答差。但人类不可能给几万、几十万条回答都打分，所以会训练一个奖励模型。

奖励模型学会之后，就可以自动给模型输出打分

**5.PPO = Proximal Policy Optimization。**

现在不用懂公式，只要知道：

> PPO 是一种用奖励来继续优化模型的方法

### 这部分最重要的一句话是：

> **CoT 可以现在用在 prompt 里；RFT 是以后如果要训练模型才考虑。**

### 二.然后讲了 LLM 在其他领域怎么用

在医学、法律、电力这些领域，大模型比较容易用，因为它们本来就有很多文字形式的数据。

但信道地图不一样。它不是纯文字任务。

所以组会上他们重点讨论：**怎么把信道地图这种“物理场”转换成 LLM 能处理的形式**。

### 三.组会重点讲了 PDE-as-Language

传统 LLM 处理的是文字序列,但物理场也可以被转换成某种“序列”

比如流体仿真里，每一时刻的流体场是一张图：

如果把每一张流体场压缩成一个 latent token，那么整个流体演化就变成了：

token1 -> token2 -> token3 -> token4

这样 LLM / Transformer 就可以像预测语言一样预测物理场的变化。

#### 换成我们信道地图，就是在想：

> **能不能把信号传播过程也变成一种“语言”？**

#### 可以简单理解为：

> **不是让 LLM 读文字，而是把物理过程编码成 token，让 Transformer 学这个物理 token 的变化规律。**

3. 这次组会对你最值得学的内容

----------------

## 值得学习之处：

### 第一，LLM 用在信道地图里有很多种位置

以前你可能只知道“用 GPT 回答问题”。

但现在你要知道，LLM 在这个方向里至少有几种用法：
  LLM as Prior Generator   输出结构化电磁先验。

LLM as Agent   控制射线追踪、经验模型、下游网络等工具。

LLM as Sequence Backbone   把信道图或环境图编码成 token，让 Transformer 预测。

LLM as Physics Token Model   把射线传播 / 物理场演化过程变成 token 序列。

* * *

### 第二，物理知识可以用三种方式进入模型

组会里其实一直在讨论一个问题：

> **物理规律到底怎么进入模型？**

有三种方式：
 方式 1：放进 prompt例如：告诉 GPT 高频更容易被遮挡、dense urban 中反射和绕射更明显。

方式 2：放进 JSON prior例如：让 GPT 输出 LOS / REF / DIFF / SCAT 权重。

方式 3：放进 token / 模型结构例如：把射线传播路径、空间网格、发射机影响编码成 token。

你现在做的是方式 1 和方式 2。邵令瑜学长讲的是方式 3。

所以你要知道：**prompt 是最容易做的，但物理约束最弱；token / 模型结构更深，但难度更大。**

* * *

### 第三，射线追踪是一个很重要的物理参考

组会里老师和学长反复提到射线追踪。原因是射线追踪天然有传播过程：
    Tx 发射→ 自由空间传播→ 遇到建筑物→ 反射 / 绕射 / 散射→ 继续传播→ 到达 Rx

这很适合用来启发 CoT 或 token 设计。

组会里也讨论了，信号从发射机到接收机的传播过程可以看成时序过程，也可以看成空间 token 过程。

你现在不需要会写射线追踪代码，但应该学会它的基本思想。

因为你后面设计 prompt、prior、Agent 的时候，都可以借鉴射线追踪的传播顺序。

* * *

### 第四，现阶段不要急着训练模型

这点非常重要。

邵令瑜学长讲到了 LoRA、RFT、PDE-as-Language，但这不代表你马上就要做微调。

你们方案文档里明确说，方案 A 第一阶段是写 system prompt、设计 Pydantic schema、手工标注 5–10 个 demonstrations、生成 prior，再接入下游模型验证。

所以你现在的主线应该是：
    先验证 LLM prior 这个想法能不能稳定产生结构化输出。

而不是：
    马上研究怎么训练一个 LLM。


