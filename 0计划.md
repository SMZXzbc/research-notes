# 课题组日常笔记 0：接下来的小计划

上周开了第一次组会，学长给出了之前阅读两篇论文后的笔记，

确定了研究方向并列举出了每一步的任务。

下面用我自己的话总结一下，并给出后续的小计划。

### 一.两篇论文回顾：

|              | LLM4PG               | RadioSim Agent      |
|:------------ |:-------------------- |:------------------- |
| **核心想法**     | 让GPT-2直接生成信号覆盖图      | 让大模型帮你操作仿真软件        |
| **大模型角色**    | **计算器** → 直接输出像素数值   | **助理** → 帮人点按钮、读结果  |
| **用到的大模型能力** | ❌ 几乎没用（输入是图像向量，不是文本） | ⚠️ 只用了一点（解析指令、描述图片） |

#### 共同的方法论问题:

两篇的共同特征是：**都把 LLM 当工具用，但都没真正利用 LLM 的语言推理能力**。

* LLM4PG 把 LLM 当**数值模型**用 → 完全浪费了预训练知识
* RadioSim 把 LLM 当**调度器**用 → 只用到了浅层的指令理解能力

**没有任何一篇把 LLM 当"会做电磁推理的领域专家"用**。

因此，可以得到我们研究的方向。

## 二.研究空隙

###### 简单来说：

**让大语言模型（LLM）当"电磁专家"，先分析场景图，输出物理先验知识，**

###### 再把这些知识喂给专门的生成模型，让它画出更准的信道地图。

## 三.完整步骤

1️⃣ 给AI立人设+塞专业知识  
2️⃣ 定义AI输出的"标准格式"  
3️⃣ 给AI看"优秀作业"学推理风格  
4️⃣ 批量生成推理结果存本地  
5️⃣ 让生成模型"听"AI的指挥  
 6️⃣ **在全新场景下验证**效果✅

## 四.小收获

阅读完论文后，他先给出了两个改进的方案。

即LLM 部分采用 prompt engineering 还是训练 LLM，

经过分析讨论，评估风险，最后选择前者。

###### 并给出一句话，感觉有点用吧：

#### 先验证，再投入——这是这类研究里最重要的执行原则。
