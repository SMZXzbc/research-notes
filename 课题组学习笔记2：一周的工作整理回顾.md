# 课题组学习笔记2：一周的工作整理回顾

这一周每天都推进了一点课题组的内容，

刚好到了一个阶段结束，

这几天便打算把这一周推进的内容用自己的话写个简单的小总结，

回顾一下整个过程。

然后再把学到的知识简单整理一下。

# 零：回顾

上周五的时候，我听完组会和学长细聊时，对我们的方向和课题组有了进一步的了解。

而且慢慢随着做的内容增多，就更加明确了自己做的内容是什么，可以讲得更加明白。

### 1.课题组的大方向：环境感知与信道地图重构

##### 简单说就是：

根据城市环境、建筑物、发射机位置、频率、少量测量数据等信息，

重构出某个区域里的无线信号分布。

##### 这个结果可以叫：Channel Knowledge Map    /  Radio Map

即：一张带有物理意义的空间信号分布图。

### 2.研究的最终目标：

##### 我们课题组目标是：把信道建模做好。

兼顾精度和速度，适应不同场景，复杂的城市环境、室内环境等等

##### 但是这个组里面每个人的切入点不一样。

# 一：我们的方向

进组的时候，老师让一位博士学长来带我，他很负责，也乐于教我。

###### 在阅读完论文后，给出了研究空隙：

###### 论文中的工作都用了 LLM，但都没有真正把 LLM 当作“会做电磁推理的专家”。

随后给出了方案A，和具体实现步骤。

### 1.方案A的简单介绍

方案 A 其实可以拆成两大块：

#### 前半段：

##### LLM 生成 structured prior

#### 后半段：

###### 把 prior 接入下游生成模型，生成 Radio Map

### 2.学长的方向：diffusion model、flow matching

下面先简单解释一下这两个概念：

##### Diffusion Model：一步步擦噪声，把图修出来

先通过慢慢加入噪声，把一张真实图慢慢弄乱，

再训练模型学会一步步把乱图还原。

##### Flow Matching：学习一条“从噪声走到真实数据”的路线。

数据怎么从噪声状态“流动”到真实地图状态

给每个点一个移动方向，  
让它从乱的位置流到正确的位置。

### 它们本质上都是生成模型

##### 即学长研究的方向是方案A的后半段：下游生成模型

### 3.我的方向：LLM prior generation

###### 即：LLM prior 如何帮助信道地图生成

#### 具体说：

###### 给生成信道地图的下游模型提供什么物理先验，又如何接入下游模型中。

即方案A的前半段，还有和下游模型的接口。

# 二：我的任务

我的目标：

##### 1.让gpt生成稳定的prior

##### 2.编码成 conditioning vector，使其和下游模型对接

下面简单解释一下这两个词：

##### prior： 就是提前知道的知识。

它在告诉下游模型：这张 Radio Map 大概应该有什么物理特征，给模型提供物理指导。

##### conditioning vector：就是把 prior 变成一串数字。

它的作用是告诉下游模型：生成 Radio Map 的时候，请参考这些条件。

# 三.具体步骤：

```
输入 case
例如：dense urban + 28GHz + street corner NLOS
        ↓
system prompt
        ↓
demonstrations
        ↓
GPT 生成 radio-map-oriented prior
        ↓
保存到 data/gpt_radiomap_priors_v2.json
        ↓
Pydantic schema 检查
        ↓
mechanism_weights 权重检查
        ↓
encode_radiomap_priors_v2.py
        ↓
生成 13 维 conditioning vector
        ↓
保存到 data/radiomap_prior_vectors_v2.json
        ↓
未来接入 diffusion / flow matching
        ↓
生成 Radio Map / Pathloss Map
```

#### 几个概念：

##### 1.system prompt

就是提前告诉 GPT：你现在要扮演谁、要做什么、必须按什么格式输出。

##### 2.demonstrations

就是给 GPT 看几个标准答案。

##### 3.prior

给 Radio Map 生成用的物理提示

##### 4.Pydantic schema

就是检查 GPT 输出是否合格的规则。

# 四：

以上便是我这一周在课题组所推进的内容，

后面会写一下在推进过程中学到的知识。
