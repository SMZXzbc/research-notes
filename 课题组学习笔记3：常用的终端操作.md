# 课题组学习笔记3：常用的终端操作

在开始写python代码前，需要搭python环境。

##### 目的：

###### 1.给这个课题单独建一个“小工作区”

###### 2.虚拟环境 `.venv` 是为了不把电脑弄乱

这个项目用自己的 Python 小环境，不影响电脑其他项目。

而且推进项目是会涉及到运行脚本等操作，

因此，需要学习一些终端的基本操作。

# 一.打开方式

### 1.CMD:Command Prompt

Windows 传统命令行,它比较简单，适合做基础操作。

##### 方法 1：Win 键+R，输入cmd

##### 方法 2：文件夹地址栏打开

在文件夹上方地址栏输入：cmd

这样打开的 CMD 会直接在当前文件夹里。

这个方法很方便。

### 2.PowerShell:更现代、更强大的 Windows 终端

##### 方法1：Win 键+R，输入powerShell

##### 方法 2：在文件夹里右键,在终端中打开

但有些命令写法会和 CMD 不一样。

比如激活虚拟环境：

CMD：

```bash
.venv\Scripts\activate
```

PowerShell：

```bash
.venv\Scripts\Activate.ps1
```

---

### 3.VS Code 里怎么打开终端？

Ctrl + `(键盘左上角 Esc 下面那个反引号。)

# 二.常用操作指令

## A、路径和文件夹相关命令

这类命令用来：

```text
查看自己在哪里
查看文件夹里有什么
进入文件夹
返回上一级
创建文件夹
```

---

#### 1.查看当前文件夹内容

```bash
dir
```

作用：

```text
查看当前文件夹下有哪些文件和文件夹。
```

例子：

```text
data
schema
scripts
prompts
README.md
```

---

#### 2.进入某个文件夹

```bash
cd 文件夹名
```

例子：

```bash
cd prior
```

作用：

```text
进入 prior 文件夹。
```

---

#### 3.返回上一级文件夹

```bash
cd ..
```

作用：

```text
回到上一级目录。
```

例子：

当前在：

```text
prior\scripts
```

输入：

```bash
cd ..
```

回到：

```text
prior
```

---

#### 4.切换到其他盘

```bash
D:
```

作用：

```text
切换到 D 盘。
```

也可以直接：

```bash
cd D:\project
```

---

#### 5.创建文件夹

```bash
mkdir 文件夹名
```

例子：

```bash
mkdir data
mkdir schema
mkdir scripts
mkdir prompts
```

作用：

```text
创建项目需要的文件夹。
```

---

#### 6.查看项目树状结构

```bash
tree
```

作用：

```text
用树状结构查看项目文件夹。
```

例子：

```text
prior
├── data
├── schema
├── scripts
├── prompts
└── README.md
```

---

#### 7.清空终端屏幕

```bash
cls
```

作用：

```text
清空终端里的旧输出。
```

注意：

```text
它不会删除文件，只是清屏。
```

---

## B、Python 环境相关命令

这类命令用来：

```text
检查 Python
创建虚拟环境
激活虚拟环境
退出虚拟环境
```

---

#### 1.检查 Python 是否安装

```bash
python --version
```

作用：

```text
查看电脑里是否安装了 Python。
```

如果输出：

```text
Python 3.13.0
```

说明 Python 可以使用。

---

#### 2.创建虚拟环境

```bash
python -m venv .venv
```

作用：

```text
给当前项目创建一个独立的 Python 环境。
```

可以理解为：

```text
.venv = 当前项目自己的 Python 小房间
```

---

#### 3.激活虚拟环境

CMD 里：

```bash
.venv\Scripts\activate
```

PowerShell 里：

```bash
.venv\Scripts\Activate.ps1
```

成功后，

终端前面一般会出现：

```text
(.venv)
```

说明虚拟环境已经激活。

---

#### 4.退出虚拟环境

```bash
deactivate
```

作用：

```text
退出当前虚拟环境。
```

---

## C、常用快捷键

#### 1.中断正在运行的程序

```text
Ctrl + C
```

作用：

```text
如果程序运行太久、卡住或进入死循环，可以停止它。
```

---

#### 2.自动补全文件名

```text
Tab
```

作用：

```text
输入文件名或文件夹名时自动补全。
```

例子：

输入：

```bash
cd scr
```

按 Tab，

可能自动变成：

```bash
cd scripts
```

---

#### 3.查看历史命令

```text
↑
↓
```

作用：

```text
找回之前输入过的命令。
```

比如刚刚运行过：

```bash
python scripts/validate_radiomap_priors_v2.py
```

按 `↑` 就能快速找回来。

---

### D.其它

##### 1.用 VS Code 打开当前项目

```bash
code .
```

作用：

```text
用 VS Code 打开当前文件夹。
```

#### 2.安装 Python 包

```bash
pip install 包名
```

例子：

```bash
pip install pydantic
```

作用：

```text
安装项目需要的 Python 包。
```

##### 3.运行普通 Python 脚本

```bash
python 脚本路径
```

例子：

```bash
python scripts/validate_priors.py
```

作用：

```text
运行 scripts 文件夹里的 Python 脚本。
```

# 三.总结

在每次推进项目的过程中，终端起的作用大概就是这条主线：

```text
进入项目
↓
激活虚拟环境
↓
安装依赖
↓
运行脚本
↓
查看结果
```
